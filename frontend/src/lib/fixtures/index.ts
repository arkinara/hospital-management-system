/**
 * Shared fixture barrel (ticket #38).
 *
 * Import everything from `@/lib/fixtures` — data, types and lookups. This is
 * the single dataset the frontend mocks and the backend seed both build on.
 */

export * from "./data";
export type * from "./types";
